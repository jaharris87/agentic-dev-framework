Review as an adversarial red-teamer: find inputs that cause silent wrong answers, malformed data that passes unnoticed, misleading but plausible output, boundary violations, and consistency invariant breaks. Propose concrete test cases for each weakness. Classify by severity (silent wrong answer > crash > degraded output). Begin with "## Red Team Review".
